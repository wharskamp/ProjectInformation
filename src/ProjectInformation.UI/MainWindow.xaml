<Window x:Class="ProjectInformation.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ProjectInformation"
        Height="620"
        Width="920"
        MinHeight="480"
        MinWidth="720">
    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom">
            <TextBlock Text="{Binding StatusMessage}" />
        </StatusBar>

        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="ProjectInformation"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Margin="0,0,0,20" />

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Text="{Binding SelectedPstFilePath, UpdateSourceTrigger=PropertyChanged}"
                         IsReadOnly="True"
                         VerticalContentAlignment="Center"
                         Margin="0,0,8,0" />
                <Button Grid.Column="1"
                        Content="Select PST file"
                        Command="{Binding SelectPstFileCommand}"
                        Padding="16,8" />
            </Grid>

            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Margin="0,24,0,0">
                <Button Content="Import"
                        Command="{Binding ImportCommand}"
                        Padding="18,8"
                        Margin="0,0,8,0" />
                <Button Content="Contacts"
                        Command="{Binding OpenContactsCommand}"
                        IsEnabled="False"
                        Padding="18,8"
                        Margin="0,0,8,0" />
                <Button Content="Export CSV"
                        Command="{Binding ExportCsvCommand}"
                        Padding="18,8" />
            </StackPanel>

            <DataGrid Grid.Row="3"
                      ItemsSource="{Binding Contacts}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      Margin="0,24,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Naam" Binding="{Binding Naam}" Width="2*" />
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2*" />
                    <DataGridTextColumn Header="Projecten" Binding="{Binding Projecten}" Width="2*" />
                    <DataGridTextColumn Header="LaatsteContact" Binding="{Binding LaatsteContact, StringFormat=yyyy-MM-dd HH:mm}" Width="*" />
                    <DataGridTextColumn Header="AantalMails" Binding="{Binding AantalMails}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</Window>
